services:
  - type: web
    name: health-prediction-ai
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
